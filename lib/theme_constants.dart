import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFB71C1C); // Deep red
  static const Color accent = Color(0xFF212121); // Deep grey
  static const Color background = Color(0xFFF5F5F5); // Light background
  static const Color appBar = Color(0xFFB71C1C);
  static const Color onAppBar = Colors.white;
  static const Color text = Color(0xFF212121);
  static const Color secondary = Color(0xFF616161);
  static const Color disabled = Color(0xFFBDBDBD);

  // Dark mode
  static const Color darkBackground = Color(0xFF18191A); // Very dark grey
  static const Color darkCard = Color(0xFF23272A); // Lighter dark for cards
  static const Color darkText = Colors.white;
  static const Color lightCard = Colors.white;
} 